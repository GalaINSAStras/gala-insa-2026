# Assets du teaser

Le carrousel utilise actuellement la vidéo existante `/video_background.mp4`
(référencée dans `src/lib/hero-data.ts`), avec le poster `/fallback_background.webp`.

> Pour revenir à un carrousel multi-slides (vidéos dédiées), déposer les médias
> ci-dessous et mettre à jour `TEASER_SLIDES` dans `src/lib/hero-data.ts`.

